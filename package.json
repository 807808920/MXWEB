{
  "name": "machine-topology",
  "version": "0.1.0",
  "private": true,
  "description": "Local and SSH machine PCIe topology explorer",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "engines": {
    "node": ">=20"
  }
}
